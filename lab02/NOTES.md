#work directory
C:\Users\user\cs-practice
#index
я хз что такое индекс
Индекс (его ещё называют staging area) — это промежуточная зона, где формируется «снимок» состояния проекта перед следующим коммитом.
#commit
я заккомитил себя в окно

git -- это квадратики такие, похожи на категории на АО3
git hub -- такой сайтик, можно найти чей-то проект для очередного костыля для линукса
Команда git status показывает текущее состояние вашего репозитория. 

#юзнутые комманды
Команда git diff в системе контроля версий Git показывает различия между разными состояниями файлов и коммитов.
git diff --staged (или --cached) — демонстрирует изменения, которые уже добавлены в индекс и готовы к коммиту. 

#ветки
ветка это некоторое отвлетвление времени созданное директором лололошкой в попытке создать идеальную ветку
при ко